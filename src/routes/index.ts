import BoardPage from '../pages/BoardPage';

const routes = [
    { path: '/', component: BoardPage, isProtected: false },
    // { path: '/tasks', component: TaskPage, isProtected: false }
]

export default routes;